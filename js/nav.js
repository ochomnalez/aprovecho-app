// Puente de navegación: main.js completa estas funciones al arrancar.
// Existe para que las pantallas puedan navegar sin importar main.js (evita ciclos).
export const nav = {
  go: (ruta, opts) => {},
  atras: () => {},
  render: () => {},
  ruta: '',
};
